pipeline {
    agent any
    
    environment {
        AWS_REGION = 'us-east-1'
        TF_VAR_aws_region = 'us-east-1'
        TF_VAR_environment = 'production'
        TF_VAR_project_name = 'nginx-web-site'
        TF_VAR_aws_account_id = '833371734412'
    }

    stages {
        stage('Check Terraform') {
            steps {
                sh '''
                    echo "🔍 Verificando instalação do Terraform..."
                    which terraform || echo "Terraform não encontrado no PATH"
                    terraform --version || echo "Comando terraform não funciona"
                    echo "📁 Listando arquivos do projeto..."
                    ls -la
                    ls -la terraform/ 2>/dev/null || echo "Diretório terraform não encontrado"
                '''
            }
        }
        
        stage('Terraform Init') {
            when {
                expression {
                    return sh(script: 'which terraform', returnStatus: true) == 0
                }
            }
            steps {
                withCredentials([aws(credentialsId: 'aws-access-key', accessKeyVariable: 'AWS_ACCESS_KEY_ID', secretKeyVariable: 'AWS_SECRET_ACCESS_KEY')]) {
                    dir('terraform') {
                        sh '''
                            echo "🔧 Inicializando Terraform..."
                            terraform init
                            echo "✅ Terraform inicializado!"
                        '''
                    }
                }
            }
        }
        
        stage('Terraform Plan') {
            when {
                expression {
                    return sh(script: 'which terraform', returnStatus: true) == 0
                }
            }
            steps {
                withCredentials([aws(credentialsId: 'aws-access-key', accessKeyVariable: 'AWS_ACCESS_KEY_ID', secretKeyVariable: 'AWS_SECRET_ACCESS_KEY')]) {
                    dir('terraform') {
                        sh '''
                            echo "📋 Executando Terraform Plan..."
                            terraform plan
                            echo "✅ Terraform Plan concluído!"
                        '''
                    }
                }
            }
        }
    }
    
    post {
        success { echo '✅ Teste do Terraform concluído com sucesso!' }
        failure { echo '❌ Teste do Terraform falhou!' }
    }
}